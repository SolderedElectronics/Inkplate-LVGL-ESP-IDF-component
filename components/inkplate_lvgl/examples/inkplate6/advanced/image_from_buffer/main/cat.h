/**
 * cat.h — placeholder for an 800x600 L8 grayscale image.
 *
 * To use your own image:
 * 1) Go to https://notisrac.github.io/FileToCArray/
 * 2) Upload your image (will be resized/converted).
 * 3) Options: Code format = Hex, Palette = 8-bit grayscale (1 byte/pixel),
 *    Multi line, Separate bytes of pixels, static + const + unsigned.
 * 4) Replace the empty array below with the generated data.
 * 5) Update CAT_WIDTH and CAT_HEIGHT to match your image.
 */

#pragma once

#define CAT_WIDTH  800
#define CAT_HEIGHT 600

static const uint8_t cat[CAT_WIDTH * CAT_HEIGHT] = {};
